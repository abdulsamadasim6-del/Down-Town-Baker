/* =========================
   DOWN TOWN BAKER
   MAIN STYLESHEET
========================= */

:root {
    --cream: #f8f1e7;
    --cream-light: #fffaf3;
    --brown: #5a3526;
    --brown-dark: #3b2118;
    --brown-light: #8b6049;
    --gold: #c49352;
    --text: #30231e;
    --muted: #756761;
    --white: #ffffff;

    --shadow: 0 15px 45px rgba(67, 40, 27, 0.12);

    --radius: 18px;

    --transition: 0.3s ease;
}


* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}


html {
    scroll-behavior: smooth;
}


body {
    font-family: "DM Sans", sans-serif;
    background: var(--cream-light);
    color: var(--text);
    line-height: 1.7;
}


a {
    text-decoration: none;
    color: inherit;
}


button {
    font-family: inherit;
}


.container {
    width: min(1150px, 90%);
    margin: auto;
}


.section {
    padding: 100px 0;
}


/* =========================
   NAVBAR
========================= */

.navbar {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1000;

    background: rgba(255, 250, 243, 0.95);

    backdrop-filter: blur(15px);

    border-bottom: 1px solid rgba(90, 53, 38, 0.08);
}


.nav-container {
    height: 82px;

    display: flex;
    align-items: center;
    justify-content: space-between;
}


.logo {
    display: flex;
    align-items: center;
    gap: 10px;

    font-family: "Playfair Display", serif;

    color: var(--brown);
}


.logo-icon {
    width: 45px;
    height: 45px;

    border-radius: 50%;

    display: grid;
    place-items: center;

    background: var(--brown);
    color: var(--cream);

    font-size: 18px;
}


.logo strong {
    display: block;
    font-size: 19px;
    line-height: 1;
}


.logo small {
    display: block;

    margin-top: 3px;

    font-family: "DM Sans", sans-serif;

    text-transform: uppercase;

    letter-spacing: 2px;

    font-size: 9px;

    color: var(--gold);
}


nav {
    display: flex;
    gap: 32px;
}


nav a {
    font-size: 14px;
    font-weight: 600;

    color: var(--brown);

    transition: var(--transition);
}


nav a:hover {
    color: var(--gold);
}


.nav-order {
    padding: 11px 20px;

    border-radius: 30px;

    background: var(--brown);

    color: white;

    font-size: 13px;
    font-weight: 700;

    transition: var(--transition);
}


.nav-order:hover {
    background: var(--gold);

    transform: translateY(-2px);
}


.menu-button {
    display: none;

    background: none;
    border: none;

    font-size: 24px;

    color: var(--brown);

    cursor: pointer;
}


/* =========================
   HERO
========================= */

.hero {
    min-height: 750px;

    position: relative;

    display: flex;
    align-items: center;

    background:
        linear-gradient(
            90deg,
            rgba(59, 33, 24, 0.92),
            rgba(59, 33, 24, 0.60),
            rgba(59, 33, 24, 0.25)
        ),
        url("https://images.unsplash.com/photo-1519869325930-281384150729?auto=format&fit=crop&w=1800&q=85");

    background-size: cover;
    background-position: center;
}


.hero-content {
    position: relative;

    padding-top: 80px;
}


.hero-text {
    max-width: 650px;

    color: white;
}


.welcome {
    display: inline-block;

    margin-bottom: 18px;

    color: #e4bd7e;

    font-size: 12px;

    font-weight: 800;

    letter-spacing: 3px;
}


.hero h1 {
    font-family: "Playfair Display", serif;

    font-size: clamp(48px, 7vw, 82px);

    line-height: 1.05;

    margin-bottom: 25px;
}


.hero h1 span {
    display: block;

    color: #e3b76f;
}


.hero p {
    max-width: 570px;

    color: #f2e7dc;

    font-size: 17px;

    margin-bottom: 35px;
}


.hero-buttons {
    display: flex;
    flex-wrap: wrap;

    gap: 14px;
}


.btn {
    display: inline-flex;

    align-items: center;
    justify-content: center;

    gap: 10px;

    padding: 14px 25px;

    border-radius: 40px;

    font-size: 14px;

    font-weight: 700;

    transition: var(--transition);
}


.primary-btn {
    background: #e2b36b;

    color: var(--brown-dark);
}


.primary-btn:hover {
    transform: translateY(-3px);

    background: white;
}


.whatsapp-btn {
    background: rgba(255,255,255,0.13);

    border: 1px solid rgba(255,255,255,0.35);

    color: white;
}


.whatsapp-btn:hover {
    background: #25d366;

    border-color: #25d366;

    transform: translateY(-3px);
}


.hero-features {
    display: flex;

    gap: 30px;

    margin-top: 45px;
}


.hero-features div {
    display: flex;

    align-items: center;

    gap: 9px;

    font-size: 12px;

    color: #f5e8da;
}


.hero-features i {
    color: #e2b36b;

    font-size: 17px;
}


/* =========================
   SECTION HEADINGS
========================= */

.section-heading {
    text-align: center;

    max-width: 650px;

    margin: 0 auto 50px;
}


.section-label {
    display: block;

    margin-bottom: 10px;

    color: var(--gold);

    font-size: 11px;

    font-weight: 800;

    letter-spacing: 3px;
}


.section-heading h2,
.about-content h2 {
    font-family: "Playfair Display", serif;

    font-size: clamp(35px, 5vw, 55px);

    line-height: 1.15;

    color: var(--brown);

    margin-bottom: 15px;
}


.section-heading h2 span,
.about-content h2 span {
    color: var(--gold);
}


.section-heading p {
    color: var(--muted);
}


/* =========================
   ABOUT
========================= */

.about {
    background: var(--cream);
}


.about-grid {
    display: grid;

    grid-template-columns: 1fr 1fr;

    align-items: center;

    gap: 90px;
}


.about-image {
    position: relative;
}


.image-card {
    padding: 15px;

    background: white;

    border-radius: 25px;

    box-shadow: var(--shadow);
}


.fake-bakery-image {
    height: 430px;

    border-radius: 18px;

    background:
        linear-gradient(
            rgba(90,53,38,0.3),
            rgba(90,53,38,0.65)
        ),
        url("https://images.unsplash.com/photo-1555507036-ab1f4038808a?auto=format&fit=crop&w=900&q=85");

    background-size: cover;
    background-position: center;

    display: flex;

    flex-direction: column;

    align-items: center;
    justify-content: center;

    color: white;
}


.fake-bakery-image i {
    font-size: 70px;

    margin-bottom: 15px;

    color: #e2b36b;
}


.fake-bakery-image p {
    font-family: "Playfair Display", serif;

    font-size: 24px;

    letter-spacing: 2px;
}


.experience {
    position: absolute;

    right: -30px;
    bottom: 30px;

    width: 150px;
    height: 150px;

    border-radius: 50%;

    background: var(--brown);

    color: white;

    display: flex;

    flex-direction: column;

    justify-content: center;
    align-items: center;

    text-align: center;

    box-shadow: var(--shadow);
}


.experience strong {
    font-family: "Playfair Display", serif;

    font-size: 30px;

    color: #e2b36b;
}


.experience span {
    font-size: 11px;

    line-height: 1.3;

    width: 90px;
}


.about-content h2 {
    margin-bottom: 25px;
}


.about-content p {
    color: var(--muted);

    margin-bottom: 15px;
}


.about-points {
    display: grid;

    grid-template-columns: 1fr 1fr;

    gap: 15px;

    margin-top: 25px;
}


.about-points div {
    display: flex;

    align-items: center;

    gap: 10px;

    font-weight: 600;

    font-size: 13px;
}


.about-points i {
    width: 24px;
    height: 24px;

    border-radius: 50%;

    display: grid;
    place-items: center;

    background: #ead9c4;

    color: var(--brown);

    font-size: 11px;
}


/* =========================
   PRODUCTS
========================= */

.products {
    background: var(--cream-light);
}


.filters {
    display: flex;

    justify-content: center;

    flex-wrap: wrap;

    gap: 10px;

    margin-bottom: 40px;
}


.filter {
    border: 1px solid #dfcdbb;

    background: white;

    color: var(--brown);

    padding: 10px 22px;

    border-radius: 30px;

    cursor: pointer;

    font-weight: 700;

    transition: var(--transition);
}


.filter:hover,
.filter.active {
    background: var(--brown);

    color: white;

    border-color: var(--brown);
}


.product-grid {
    display: grid;

    grid-template-columns: repeat(3, 1fr);

    gap: 25px;
}


.product-card {
    background: white;

    border-radius: var(--radius);

    overflow: hidden;

    box-shadow: 0 5px 25px rgba(67,40,27,0.07);

    transition: var(--transition);
}


.product-card:hover {
    transform: translateY(-8px);

    box-shadow: var(--shadow);
}


.product-image {
    height: 245px;

    position: relative;

    overflow: hidden;
}


.product-placeholder {
    height: 100%;

    display: grid;
    place-items: center;

    background:
        linear-gradient(
            135deg,
            #ead8c5,
            #c9a383
        );

    color: var(--brown);

    font-size: 65px;

    transition: transform 0.5s ease;
}


.product-card:hover .product-placeholder {
    transform: scale(1.06);
}


.badge {
    position: absolute;

    top: 15px;
    left: 15px;

    background: var(--brown);

    color: white;

    padding: 6px 12px;

    border-radius: 20px;

    font-size: 10px;

    font-weight: 800;

    text-transform: uppercase;

    letter-spacing: 1px;
}


.product-info {
    padding: 22px;
}


.category {
    color: var(--gold);

    font-size: 10px;

    font-weight: 800;

    letter-spacing: 2px;
}


.product-info h3 {
    font-family: "Playfair Display", serif;

    font-size: 24px;

    color: var(--brown);

    margin: 5px 0;
}


.product-info p {
    font-size: 13px;

    color: var(--muted);

    margin-bottom: 18px;
}


.product-bottom {
    display: flex;

    align-items: center;
    justify-content: space-between;
}


.product-bottom strong {
    color: var(--brown);

    font-size: 12px;
}


.product-bottom button {
    width: 40px;
    height: 40px;

    border: none;

    border-radius: 50%;

    background: #e9dfd4;

    color: var(--brown);

    cursor: pointer;

    font-size: 17px;

    transition: var(--transition);
}


.product-bottom button:hover {
    background: #25d366;

    color: white;

    transform: scale(1.08);
}


.all-items {
    text-align: center;

    margin-top: 50px;
}


.all-items p {
    color: var(--muted);

    margin-bottom: 12px;
}


.outline-btn {
    border: 1px solid var(--brown);

    color: var(--brown);
}


.outline-btn:hover {
    background: var(--brown);

    color: white;
}


/* =========================
   WHY US
========================= */

.why-us {
    background:
        linear-gradient(
            rgba(59,33,24,0.96),
            rgba(59,33,24,0.96)
        ),
        url("https://images.unsplash.com/photo-1509440159596-0249088772ff?auto=format&fit=crop&w=1600&q=80");

    background-size: cover;

    background-attachment: fixed;
}


.section-heading.light h2 {
    color: white;
}


.section-heading.light p {
    color: #ddcfc5;
}


.why-grid {
    display: grid;

    grid-template-columns: repeat(4, 1fr);

    gap: 20px;
}


.why-card {
    text-align: center;

    padding: 30px 20px;

    border: 1px solid rgba(255,255,255,0.12);

    border-radius: 18px;

    background: rgba(255,255,255,0.05);

    transition: var(--transition);
}


.why-card:hover {
    background: rgba(255,255,255,0.1);

    transform: translateY(-6px);
}


.why-icon {
    width: 65px;
    height: 65px;

    margin: 0 auto 18px;

    display: grid;
    place-items: center;

    border-radius: 50%;

    background: rgba(226,179,107,0.15);

    color: #e2b36b;

    font-size: 25px;
}


.why-card h3 {
    color: white;

    font-family: "Playfair Display", serif;

    font-size: 20px;

    margin-bottom: 8px;
}


.why-card p {
    color: #cfc0b6;

    font-size: 12px;
}


/* =========================
   CTA
========================= */

.cta {
    padding: 65px 0;

    background: #e2b36b;
}


.cta-content {
    display: flex;

    align-items: center;
    justify-content: space-between;

    gap: 30px;
}


.cta span {
    color: var(--brown-dark);

    font-size: 11px;

    font-weight: 800;

    letter-spacing: 2px;
}


.cta h2 {
    font-family: "Playfair Display", serif;

    font-size: clamp(30px, 4vw, 45px);

    color: var(--brown-dark);

    line-height: 1.15;

    margin-top: 5px;
}


.cta h2 em {
    color: white;

    font-style: normal;
}


.cta-btn {
    background: var(--brown);

    color: white;

    white-space: nowrap;
}


.cta-btn:hover {
    background: white;

    color: var(--brown);

    transform: translateY(-3px);
}


/* =========================
   CONTACT
========================= */

.contact {
    background: var(--cream);
}


.contact-grid {
    display: grid;

    grid-template-columns: repeat(4, 1fr);

    gap: 18px;
}


.contact-card {
    text-align: center;

    background: white;

    border-radius: 18px;

    padding: 30px 18px;

    box-shadow: 0 5px 25px rgba(67,40,27,0.05);
}


.contact-icon {
    width: 55px;
    height: 55px;

    margin: 0 auto 15px;

    display: grid;
    place-items: center;

    border-radius: 50%;

    background: #ead9c4;

    color: var(--brown);

    font-size: 20px;
}


.contact-card h3 {
    font-family: "Playfair Display", serif;

    color: var(--brown);

    margin-bottom: 5px;
}


.contact-card p {
    color: var(--muted);

    font-size: 12px;

    margin-bottom: 8px;
}


.contact-card a {
    color: var(--gold);

    font-weight: 700;

    font-size: 12px;
}


/* =========================
   FOOTER
========================= */

footer {
    background: var(--brown-dark);

    color: #ddcfc5;

    padding-top: 70px;
}


.footer-grid {
    display: grid;

    grid-template-columns: 2fr 1fr 1fr 1fr;

    gap: 50px;

    padding-bottom: 50px;
}


.footer-brand .logo {
    color: white;

    margin-bottom: 18px;
}


.footer-brand p {
    font-size: 13px;

    max-width: 280px;
}


footer h4 {
    color: #e2b36b;

    margin-bottom: 15px;

    font-size: 14px;
}


footer > .container > div > a,
footer > .container > div > p {
    display: block;

    margin-bottom: 7px;

    font-size: 12px;

    color: #cbbdb4;
}


footer a:hover {
    color: #e2b36b !important;
}


.socials {
    display: flex;

    gap: 9px;

    margin-top: 20px;
}


.socials a {
    width: 38px;
    height: 38px;

    border: 1px solid rgba(255,255,255,0.15);

    border-radius: 50%;

    display: grid;
    place-items: center;

    transition: var(--transition);
}


.socials a:hover {
    background: #e2b36b;

    color: var(--brown-dark) !important;
}


.copyright {
    border-top: 1px solid rgba(255,255,255,0.1);

    text-align: center;

    padding: 20px;

    font-size: 11px;

    color: #a99b92;
}


/* =========================
   FLOATING WHATSAPP
========================= */

.floating-whatsapp {
    position: fixed;

    right: 25px;
    bottom: 25px;

    width: 58px;
    height: 58px;

    border-radius: 50%;

    display: grid;
    place-items: center;

    background: #25d366;

    color: white;

    font-size: 27px;

    box-shadow: 0 8px 25px rgba(37,211,102,0.35);

    z-index: 999;

    transition: var(--transition);
}


.floating-whatsapp:hover {
    transform: scale(1.1);
}


/* =========================
   RESPONSIVE
========================= */

@media (max-width: 950px) {

    nav {
        gap: 15px;
    }

    .product-grid {
        grid-template-columns: repeat(2, 1fr);
    }

    .why-grid,
    .contact-grid {
        grid-template-columns: repeat(2, 1fr);
    }

    .about-grid {
        gap: 40px;
    }

}


@media (max-width: 750px) {

    .nav-order {
        display: none;
    }

    .menu-button {
        display: block;
    }

    nav {
        position: absolute;

        top: 82px;
        left: 0;

        width: 100%;

        padding: 20px;

        background: var(--cream-light);

        flex-direction: column;

        gap: 0;

        display: none;

        box-shadow: 0 15px 20px rgba(0,0,0,0.08);
    }

    nav.show {
        display: flex;
    }

    nav a {
        padding: 12px;

        border-bottom: 1px solid #eaded2;
    }

    .hero {
        min-height: 700px;
    }

    .hero h1 {
        font-size: 52px;
    }

    .hero-features {
        flex-direction: column;

        gap: 12px;
    }

    .about-grid {
        grid-template-columns: 1fr;
    }

    .about-image {
        max-width: 600px;

        margin: auto;
    }

    .product-grid {
        grid-template-columns: 1fr;
    }

    .why-grid,
    .contact-grid {
        grid-template-columns: 1fr 1fr;
    }

    .footer-grid {
        grid-template-columns: 1fr 1fr;

        gap: 35px;
    }

    .cta-content {
        flex-direction: column;

        text-align: center;
    }

}


@media (max-width: 480px) {

    .section {
        padding: 70px 0;
    }

    .hero h1 {
        font-size: 43px;
    }

    .hero p {
        font-size: 15px;
    }

    .hero-buttons {
        flex-direction: column;
    }

    .btn {
        width: 100%;
    }

    .about-points {
        grid-template-columns: 1fr;
    }

    .why-grid,
    .contact-grid {
        grid-template-columns: 1fr;
    }

    .footer-grid {
        grid-template-columns: 1fr;
    }

    .experience {
        right: 5px;
        bottom: 20px;

        width: 120px;
        height: 120px;
    }

}